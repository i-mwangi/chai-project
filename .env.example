# Example environment variables
# Set to 'true' to disable investor KYC/verification (auto-approves investors)
DISABLE_INVESTOR_KYC=false

# Admin token used by admin endpoints (replace in production)
ADMIN_TOKEN=admin-secret-token

# Other env variables used by the app (DB, ports, etc.)
# DATABASE_URL=postgres://user:pass@localhost:5432/dbname
# PORT=3001